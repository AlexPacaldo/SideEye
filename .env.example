# Optional: connect to a Supabase project for online multiplayer + accounts.
# When left empty the app runs fully offline using the built-in local backend
# (single-device, pass-and-play). Copy this file to `.env` to configure.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
